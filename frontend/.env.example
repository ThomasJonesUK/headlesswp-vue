# Set to 'true' to use mock data instead of WordPress API
# Useful for testing the frontend without WordPress running
VITE_USE_MOCK_DATA=false

# WordPress GraphQL endpoint
VITE_GRAPHQL_ENDPOINT=http://localhost:8080/graphql
